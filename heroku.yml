setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
build:
  docker:
      web: Dockerfile
release:
  command:
    - ASPNETCORE_URLS=http://*:$PORT dotnet CoreInvestmentTracker.dll
  image: worker
